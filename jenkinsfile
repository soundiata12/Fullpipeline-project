pipeline{
    agent any
    stages{
        stage('checkout scm'){
            steps{
                checkout([
                    branch: [['*/main']]
                    url: 'https://github.com/soundiata12/Fullpipeline-project.git'branch
                ])
            }
            
            }
        }
    }
